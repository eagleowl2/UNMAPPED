# Backend FastAPI base URL (Claude 1's service exposing /parse).
# Falls back to mock parser when unset or unreachable.
VITE_API_URL=http://localhost:8000

# Demo mode forces use of the bundled mock parser even if API is reachable.
# Useful when presenting to judges with flaky wifi.
VITE_DEMO_MODE=false

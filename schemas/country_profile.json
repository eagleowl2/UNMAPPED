{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://unmapped.protocol/schemas/v0.2/country_profile.json",
  "title": "CountryProfile",
  "description": "UNMAPPED Protocol v0.2 Section 4 — Country/context configuration for localised skill taxonomy, language support, and delivery channels.",
  "type": "object",
  "required": ["country_code", "country_name", "languages", "informal_economy", "delivery_channels", "taxonomy_config"],
  "additionalProperties": false,
  "properties": {
    "country_code": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "ISO 3166-1 alpha-2 country code"
    },
    "country_name": {
      "type": "string"
    },
    "region": {
      "type": "string",
      "enum": ["SSA", "MENA", "SASIA", "EASIA", "LAC", "GLOBAL"]
    },
    "context_tag": {
      "type": "string",
      "description": "e.g. 'urban_informal', 'rural_agricultural', 'peri_urban'"
    },
    "languages": {
      "type": "object",
      "required": ["primary", "supported"],
      "properties": {
        "primary": {
          "type": "string",
          "description": "BCP-47 language tag"
        },
        "supported": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "BCP-47 tags of all supported languages"
        },
        "low_resource": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Languages requiring fallback/transliteration support"
        }
      }
    },
    "informal_economy": {
      "type": "object",
      "required": ["enabled", "sector_tags"],
      "properties": {
        "enabled": { "type": "boolean" },
        "gdp_share_pct": { "type": "number", "minimum": 0, "maximum": 100 },
        "sector_tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Dominant informal sectors, e.g. ['mobile_repair', 'street_trade', 'agri_processing']"
        },
        "zero_credential_default": {
          "type": "boolean",
          "description": "If true, all skills default to zero-credential evidence chain"
        }
      }
    },
    "delivery_channels": {
      "type": "object",
      "required": ["sms", "ussd"],
      "properties": {
        "sms": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "max_chars": { "type": "integer", "default": 160 },
            "provider": { "type": "string" }
          }
        },
        "ussd": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "max_depth": { "type": "integer", "default": 3 },
            "menu_char_limit": { "type": "integer", "default": 182 }
          }
        },
        "web": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "spa_url": { "type": "string", "format": "uri" }
          }
        },
        "whatsapp": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" }
          }
        }
      }
    },
    "taxonomy_config": {
      "type": "object",
      "required": ["primary_framework", "crosswalk_targets"],
      "properties": {
        "primary_framework": {
          "type": "string",
          "enum": ["ISCO-08", "O*NET", "ESCO", "NAICS", "LOCAL"],
          "description": "Primary occupational taxonomy"
        },
        "crosswalk_targets": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["ISCO-08", "O*NET", "ESCO", "SDG", "ILO_STAT"]
          }
        },
        "local_skill_overrides": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["local_label", "canonical_code", "framework"],
            "properties": {
              "local_label": { "type": "string" },
              "canonical_code": { "type": "string" },
              "framework": { "type": "string" }
            }
          }
        },
        "skill_alias_registry": {
          "type": "array",
          "description": "v0.3.1 — locale-specific multilingual aliases (Twi/Ga/Armenian/Russian/English). Each entry maps any of `aliases` (exact, case-insensitive, Unicode-NFC) to a canonical skill record used by the parser BEFORE regex/embedding fallbacks.",
          "items": {
            "type": "object",
            "required": ["canonical_label", "aliases", "isco_code", "category"],
            "additionalProperties": false,
            "properties": {
              "canonical_label": {
                "type": "string",
                "description": "Display label used in the ProfileCard (English by default; rendered via locale layer)."
              },
              "aliases": {
                "type": "array",
                "minItems": 1,
                "items": { "type": "string" },
                "description": "List of multilingual surface forms — Twi/Ga/Armenian/Russian/English/transliterated. Matched as case-insensitive substrings with Unicode NFC normalisation."
              },
              "isco_code": {
                "type": "string",
                "pattern": "^[0-9]{3,4}$",
                "description": "ISCO-08 4-digit (or 3-digit) code."
              },
              "category": {
                "type": "string",
                "enum": ["technical", "digital", "trade", "creative", "financial", "care", "agricultural", "other"]
              },
              "base_weight": {
                "type": "number",
                "minimum": 0.0,
                "maximum": 1.0,
                "default": 0.78,
                "description": "Evidence weight applied when this alias matches. Locale-specific aliases (Twi/Ga/Armenian) are typically 0.80+ because the surface form is unambiguous."
              },
              "language": {
                "type": "string",
                "description": "ISO 639-1/-3 hint (e.g. ak, gaa, hy, ru, en) — used for telemetry only, not gating."
              }
            }
          }
        }
      }
    },
    "confidence_priors": {
      "type": "object",
      "description": "Bayesian priors for this context (Section 5.3)",
      "properties": {
        "self_report_alpha": { "type": "number", "default": 2.0 },
        "self_report_beta": { "type": "number", "default": 3.0 },
        "peer_endorsement_weight": { "type": "number", "minimum": 0, "maximum": 1 },
        "transaction_evidence_weight": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    },
    "profile_card": {
      "type": "object",
      "description": "Human Layer profile card display config (Section 7)",
      "properties": {
        "template": { "type": "string" },
        "badge_color_scheme": { "type": "string" },
        "currency_symbol": { "type": "string" },
        "phone_prefix": { "type": "string" }
      }
    }
  }
}
